using System;

namespace LibPlugin
{
    //public abstract class DelegateHandler
    //{
    //    protected Delegate Delegate { get; set; }

    //    public object Invoke(params object[] param)
    //    {
    //        return Delegate?.Method.GetParameters().Length == param.Length ? Delegate?.DynamicInvoke(param) : null;
    //    }
    //}
}